<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 上限額管理の配分方式
    |--------------------------------------------------------------------------
    |
    | priority     : 管理事業所優先充当方式（国保連の上限額管理事務の標準）
    | proportional : 比例按分方式（自治体が按分を指定している場合のみ）
    |
    | 指定権者（自治体）の取扱いを確認のうえ設定すること。
    */

    'cap_allocation_method' => env('BILLING_CAP_ALLOCATION_METHOD', 'priority'),

    /*
    |--------------------------------------------------------------------------
    | 請求関連出力ファイルの保持日数
    |--------------------------------------------------------------------------
    |
    | storage/app/billing_csv 等に生成される CSV/PDF/ZIP の保持日数。
    | 経過したファイルは billing:cleanup-exports コマンドで削除される。
    | （billing_exports の履歴レコードは残る。再出力は可能）
    */

    'export_retention_days' => env('BILLING_EXPORT_RETENTION_DAYS', 90),
];
